<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luyublog.aidemo.infrastructure.persistence.mysql.WatchdogMapper">

    <insert id="insert">
        INSERT INTO watchdog (message_id, conversation_id, created_at)
        VALUES (#{messageId}, #{conversationId}, #{createdAt})
    </insert>

    <delete id="delete">
        DELETE
        FROM watchdog
        WHERE message_id = #{messageId}
    </delete>

    <select id="findScannable" resultType="java.lang.String">
        SELECT message_id
        FROM watchdog
        WHERE created_at &lt; #{before}
        ORDER BY created_at
        LIMIT #{limit}
    </select>

</mapper>
